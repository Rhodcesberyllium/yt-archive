# 一劳永逸云抓取 · 部署指南（免梯子版）

这套东西 = **把你的抓取任务搬到 GitHub 的免费云服务器上跑**。GitHub 的服务器在美国，直连
YouTube 没有墙，所以不再需要 VPN、不再依赖时好时坏的中继、不再等深夜窗口。
**成本 0 元；一次性布置约 10 分钟；之后每周自动全量抓取，永远不用管。**

> 本文件夹（`fetch.py`、`verify_output.py`、`.github/`）整体传到你的 GitHub 仓库即可。
> 抓取结果会自动写回仓库里的 `data/<频道>_videos.txt`，你在国内随时点开下载。

---

## 第一部分：把代码放进 GitHub（一次性，约 10 分钟）

### 第 0 步 登录
浏览器打开 <https://github.com> 用你的账号登录。

### 第 1 步 建一个仓库（"仓库"= 挂在 GitHub 上的免费文件夹）
1. 页面右上角点绿色的 **New** 按钮
2. **Repository name** 填：`yt-archive`
3. 选 **Public**（公有，抓取时长不限额；数据本就是公开信息，也能被搜索引擎看到，介意可转私有，见"常见问题"）
4. 点绿色 **Create repository**（不要选任何 template / README，空仓库即可）

### 第 2 步 把本文件夹的内容传上去
1. 在刚建好的仓库页，点 **Add file** → **Upload files**
2. 打开本机这个 `github-actions` 文件夹，**把里面的内容全部拖进上传框**
   （用户 `fetch.py`、`verify_output.py`、`.gitignore`、**以及 `.github` 文件夹**——隐藏文件夹也要一起拖进去，它里面是定时任务配置）
3. 拖进去后点绿色 **Commit changes**

ⓘ 如果 `.github` 文件夹拖不进去（个别浏览器会漏掉隐藏文件夹）：
点 **Add file → Create new file**，文件名直接输入 `.github/workflows/fetch.yml`，
把本机 `fetch.yml` 的内容粘贴进去，点 Commit 即可。

### 第 3 步 等它自己跑起来（或手动触发）
- 上传时 **push 事件会自动触发第一次抓取**。等 2~4 分钟。
- 如果没自动跑：点仓库顶部 **Actions** 标签 → 左侧蓝字 **fetch-youtube-cloud** →
  右侧 **Run workflow** → 绿色按 **Run workflow** 按钮。
- 页面出现一个黄色（运行中）的圆圈，变成绿色 ✓ = 成功。

---

## 第二部分：拿结果（以后每次只需要这一步）

### 方式 A：在仓库里直接看
仓库首页 → `data` 文件夹 → `<频道>_videos.txt` → 点开就能看，右侧 **Download** 可下载。

### 方式 B：永久下载链接（推荐，可收藏）
把下面链接里的 `你的用户名` 和 `yt-archive` 换掉即可，随时打开就是最新清单：

```
https://cdn.jsdelivr.net/gh/你的用户名/yt-archive@main/data/NurdRage_videos.txt
```

- 这条链接走 jsDelivr 全球 CDN，国内一般可以直接打开。
- 每周一自动跑一次后会自动更新。你也可以在 Actions 页点 **Run workflow** 随时手动刷新。

---

## 换频道（可选）

默认抓 NurdRage。要换别的频道：
仓库页 → **Settings** → 左侧 **Secrets and variables → Actions → Variables** →
**New repository variable**：
- Name：`CHANNEL_URL`
- Value：你想抓的频道链接，例如 `https://www.youtube.com/@某频道/videos`

保存后在 Actions 页手动跑一次即可。视频清单文件名会自动变成新频道名。

---

## 常见问题

**Q1：真的完全免费吗？**
是的。公有仓库的 GitHub Actions 时间不限量；抓一次约几分钟到 20 分钟（视频多、要逐条取官方发布日），
每周才跑一次，用量极小。

**Q2：抓取会不会触发 YouTube 风控？**
单频道、每周一次、纯公开信息，属于正常低频访问。即使某次被限流也没关系：
工作流会报错，下周自动重试；你也可以随时手动重跑。

**Q3：抓到的日期准不准？会不会又出现"假日期"？**
不会。云端版沿用和本地版完全一致的诚实精度政策：
- 最新约 15 条 → 官方 RSS **精确到秒**
- 其余全部 → 官方 `upload_date` **精确到日**（这是你要的默认档）
- 个别拿不到的 → 标"未知"，下周自动重试
每个结果都过 `verify_output.py` 自动验收，严禁伪造精度。

**Q4：公有仓库别人能看到我的清单？**
能搜到，但内容只是频道的公开视频信息。如果你介意，随时可以在 Settings → General →
Danger Zone → Change repository visibility 一键转私有。
（提醒：私有仓库的定时任务在 60 天无任何活动后会被 GitHub 暂停，转私后最好隔一两个月手动 Run 一次。）

**Q5：和本机那套 `youtube_fetcher.py` 是什么关系？**
同一个思路、同样的输出格式和验收标准，只是"在哪跑"从你的电脑换成了 GitHub 云端——
墙的问题在云端不存在，覆盖率可以做到 100%（全量枚举），而不再受限于中继窗口。

---

## 文件说明
- `fetch.py` —— 云端全量抓取脚本（枚举 videos/shorts/streams 三个 tab → 官方发布日 → 官方 RSS 精确秒 → 双路合并写盘）
- `verify_output.py` —— 自动验收脚本（与本地版同一份，格式/精度/排序/去重全查）
- `.github/workflows/fetch.yml` —— 定时任务：每周一 UTC 10:00 自动跑 + 支持手动触发
- `.gitignore` —— 忽略缓存，仓库保持干净
