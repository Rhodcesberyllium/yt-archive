# 一劳永逸云抓取 · 部署指南（免梯子版）

这套东西 = **把你的抓取任务搬到 GitHub 的免费云服务器上跑**。GitHub 的服务器在美国，直连
YouTube 没有墙，所以不再需要 VPN、不再依赖时好时坏的中继、不再等深夜窗口。
**成本 0 元；一次性布置约 10 分钟；之后每周自动全量抓取，永远不用管。**

> 需要上传到仓库的东西：`fetch.py`、`verify_output.py`、`channels.txt`、`.github/`。
> 抓取结果会自动写回仓库里的 `data/<频道>_videos.txt`，你在国内随时点开下载。

**如果仓库已经建好了**（本仓库就是），你只需要关心两件事：
1. 想抓哪些频道 → 改根目录的 `channels.txt`；
2. 想看结果 → 打开 `data/` 里的 `xxx_videos.txt`。

下面是从零开始的完整步骤，第一次搭建时才需要看。

---

## 第一部分：把代码放进 GitHub（一次性，约 10 分钟）

### 第 0 步 登录
浏览器打开 <https://github.com> 用你的账号登录。

### 第 1 步 建一个仓库（"仓库"= 挂在 GitHub 上的免费文件夹）
1. 页面右上角点绿色的 **New** 按钮
2. **Repository name** 填：`yt-archive`
3. 选 **Public**（公有，抓取时长不限额；数据本就是公开信息。介意可转私有，见"常见问题"）
4. 点绿色 **Create repository**（不要选任何 template / README，空仓库即可）

### 第 2 步 把文件传上去
1. 在刚建好的仓库页，点 **Add file** → **Upload files**
2. 把本文件夹里的这些拖进上传框：
   - `fetch.py`（抓取主程序）
   - `verify_output.py`（验收自检）
   - `channels.txt`（**频道清单，你以后要改的就是这个**）
   - `.gitignore`
   - **`.github` 文件夹**（隐藏文件夹也要一起拖进去，里面是定时任务配置）
3. 拖进去后点绿色 **Commit changes**

ⓘ 如果 `.github` 文件夹拖不进去（个别浏览器会漏掉隐藏文件夹）：
点 **Add file → Create new file**，文件名直接输入 `.github/workflows/fetch.yml`，
把本机 `fetch.yml` 的内容粘贴进去，点 Commit 即可。

### 第 3 步 等它自己跑起来（或手动触发）
- 上传时 **push 事件会自动触发第一次抓取**。等几分钟到半小时（取决于频道视频数量）。
- 如果没自动跑：点仓库顶部 **Actions** 标签 → 左侧蓝字 **fetch-youtube-cloud** →
  右侧 **Run workflow** → 绿色 **Run workflow** 按钮。
- 页面出现一个黄色（运行中）的圆圈，变成绿色 ✓ = 成功。

---

## 脚本更新 / 手动重跑（结果不对时用）

- **更新脚本**：仓库页 → **Add file → Upload files** → 把新的 `fetch.py` 拖进去 → **Commit changes**。
  只要改了 `fetch.py`，就会自动触发一次重抓。
- **手动重跑**：Actions 页 → 选最新一次运行 → 右侧 **Re-run all jobs**。
- **临时抓一个新频道**：Actions 页 → **Run workflow** → 在 `channel_url` 表单里粘频道链接 → 运行。
  那次会"`channels.txt` 里的全部频道 + 你填的这个"一起抓，新频道的清单会出现在 `data/` 里。

### 怎么判断跑得好不好

**最省事的办法**：打开 `data/_run_log.txt`，那是最近一次运行的完整日志，里面有每一步的进度和失败原因。

也可以直接看输出文件底部的「统计」和「数据通道明细」，重点看这几行：

- `可靠日期(精确到秒或日)合计: X / N 条 = Y%` —— 越高越好
- `其中日期未知: Z 条` —— 越少越好，会逐周自动补
- `数据通道明细` 里每条来源各贡献了多少条，比如
  `- 官方 RSS（精确到秒）: 15 条`、`- archive.org 存档快照（日精度）: 60 条`、
  `- Piped 镜像（官方时间戳）: 0 条`
- `播放接口客户端探测（命中/样本/状态）` —— 如果全是 `0/6(LOGIN_REQUIRED)` 之类，
  说明该次运行被 YouTube 风控了，下周会重试。

### 输出文件名

以频道真实名命名，例如 `data/NurdRage_videos.txt`、`data/Explosions&Fire_videos.txt`。
换了频道就用新频道名，记得同步更新你收藏的下载链接。
（顺带说明：早期版本会把频道名里的 `&` 写成 `&amp;`，导致出现
`Explosions&amp;Fire_videos.txt` 这种看着别扭的文件名，现在已修正。）

---

## 第二部分：拿结果（以后每次只需要这一步）

### 方式 A：在仓库里直接看
仓库首页 → `data` 文件夹 → `<频道>_videos.txt` → 点开就能看，右侧 **Download** 可下载。

### 方式 B：永久下载链接（推荐，可收藏）
把下面链接里的名字换成你的：

```
https://cdn.jsdelivr.net/gh/Rhodcesberyllium/yt-archive@main/data/NurdRage_videos.txt
```

- 这条链接走 jsDelivr 全球 CDN，国内一般可以直接打开。
- 每周一自动跑一次后会自动更新。也可以在 Actions 页点 **Run workflow** 随时手动刷新。

---

## 换频道 / 加频道（推荐做法）

编辑仓库根目录的 `channels.txt`，一行一个频道链接：

```
https://www.youtube.com/@NurdRage/videos
https://www.youtube.com/@NileRed/videos
https://www.youtube.com/@你要加的频道/videos
```

- 以 `#` 开头的整行会被忽略，行尾也能写 `# 注释`
- 链接带不带 `/videos` 后缀效果一样
- **每次运行会把这份清单里的全部频道一起刷新**，不用一个个手动跑

另有一种方式（不推荐，只对单次生效）：仓库页 → **Settings** →
**Secrets and variables → Actions → Variables** → **New repository variable**，
Name 填 `CHANNEL_URL`、Value 填频道链接。这只影响"表单留空时抓哪一个"，不会替代 `channels.txt`。

---

## 可选：让日期全部精确到秒

默认走**免 key** 路线（官方 RSS + 公开镜像 + archive.org 存档 + 播放接口 + 相对时间兜底），
不用配任何东西，但部分老视频只能推断到年。

想要**每一条都精确到秒**，加一个免费的官方 API key（5 分钟，一次性）：

1. <https://console.cloud.google.com/> → 建项目
2. **APIs & Services → Library** → 搜 **YouTube Data API v3** → **Enable**
3. **APIs & Services → Credentials** → **Create credentials → API key** → 复制
4. 仓库 → **Settings → Secrets and variables → Actions → Secrets → New repository secret**
   - Name：`YT_API_KEY`，Secret：粘贴刚才的 key
5. Actions 页 **Run workflow** 跑一次

配额：每 50 个视频 1 个单位，每天免费 10000 个单位。几千个视频一次全量只用几十个单位，
**完全免费**。不想用了删掉这个 Secret 即自动退回免 key 路线。

---

## 常见问题

**Q1：真的完全免费吗？**
是的。公有仓库的 GitHub Actions 时间不限量；抓一次几分钟到半小时，每周才跑一次，用量极小。

**Q2：抓取会不会触发 YouTube 风控？**
单频道、每周一次、纯公开信息，属于正常低频访问。即使某次被限流也没关系：
工作流会报错，下周自动重试；已抓到的日期存在缓存里不会丢。

**Q3：抓到的日期准不准？会不会出现"假日期"？**
不会，这是这套工具最在意的一点：
- 最新约 15 条 → 官方 RSS，**精确到秒**
- 配了 `YT_API_KEY` → 全部官方接口，**精确到秒**
- 其余按可信度取镜像/存档/播放接口的**官方日期（精确到日）**
- 再拿不到就标"推断（±1年，来自页面相对时间）"或"未知"，并给出按视频 ID 时序推算的区间提示
每个结果都过 `verify_output.py` 自动验收，**出现伪精度日期会直接判定失败**。

**Q4：为什么不是所有视频都立即有精确日期？**
GitHub 的服务器 IP 属于数据中心 IP，YouTube 对"逐个视频查详情"这类访问整体风控很严。
所以工具靠的是**频道级批量接口 + 历史存档 + 逐周缓存累积**：一次补不全，下一周继续补，
补到的高精度日期永久保留。跑几周后覆盖率会明显提升；配上 `YT_API_KEY` 则一次到位。

**Q5：公有仓库别人能看到我的清单？**
能搜到，但内容只是频道的公开视频信息。介意就 Settings → General → Danger Zone →
Change repository visibility 一键转私有。
（提醒：私有仓库的定时任务在 60 天无任何活动后会被 GitHub 暂停，转私后最好隔一两个月手动 Run 一次。）

**Q6：和本机那套 `youtube_fetcher.py` 是什么关系？**
同一个思路、同样的输出格式和验收标准，只是"在哪跑"从你的电脑换成了 GitHub 云端——
墙的问题在云端不存在，覆盖率可以做到 100%（全量枚举）。

---

## 文件说明

| 文件 | 作用 |
| --- | --- |
| `channels.txt` | **频道清单**，一行一个频道，你要改的就是这个 |
| `fetch.py` | 云端抓取主程序：枚举三 tab → 多源日期解析 → 归档比对 → 写清单与缓存 |
| `verify_output.py` | 自动验收脚本：格式 / 精度 / 排序 / 去重全查，不过就报错 |
| `.github/workflows/fetch.yml` | 定时任务：每周一 UTC 10:00 自动跑 + 支持手动触发与表单填频道 |
| `data/<频道>_videos.txt` | 抓取结果（人读，含统计与数据通道明细） |
| `data/<频道>_dates.txt` | 日期缓存（机器维护，**别手动改**） |
| `data/_run_log.txt` | 最近一次运行的完整日志（排障用） |
| `.gitignore` | 忽略缓存目录，仓库保持干净 |
